# Random 随机决策应用 — 面试准备文档

> **项目定位**：一个基于 Spring Boot + React 的全栈决策辅助平台，集成加权随机算法与 AI 大模型，帮助用户在日常场景中做出高效决策。
>
> **目标岗位**：Java 后端开发实习生

---

## 一、项目亮点（30 秒自我介绍用）

> "我独立设计并实现了一个全栈决策应用 Random。后端基于 Spring Boot 3.2，采用分层架构 + JWT 无状态认证，核心是一个**加权随机决策引擎**——通过用户反馈动态调整标签权重，实现'越用越懂你'的偏好学习。同时集成了小米 MiMo 大模型，提供智能推荐、偏好分析等 AI 能力。项目已部署上线，前端 Vercel + 后端 Render + PostgreSQL，支持生产环境访问。"

---

## 二、技术栈全景

```
┌──────────────────────────────────────────────────────────┐
│                        前端                               │
│  React 18 + TypeScript + Vite 6 + Tailwind CSS v4       │
│  React Router v7 + Framer Motion + Axios + Zod           │
│                          │                                │
│                     HTTPS / REST API                      │
├──────────────────────────────────────────────────────────┤
│                        后端                               │
│  Spring Boot 3.2.5 + Spring Security + Spring Data JPA   │
│  JWT (JJWT 0.12.5) + BCrypt + Bean Validation            │
│  LLM Integration (OpenAI-compatible API)                 │
├──────────────────────────────────────────────────────────┤
│                      数据层                               │
│  MySQL 8.0 (开发) / PostgreSQL 16 (生产)                  │
│  Hibernate 6 + ddl-auto=update                           │
├──────────────────────────────────────────────────────────┤
│                      部署                                 │
│  Docker 多阶段构建 + Render (后端) + Vercel (前端)         │
└──────────────────────────────────────────────────────────┘
```

---

## 三、数据库设计

### 3.1 ER 关系图

```
users ──────┐
  │         │
  │ 1:N     │ 1:N
  ▼         ▼
options ◄── decisions ──1:1──► feedback
  │                               │
  │ N:1                           │ N:1
  ▼                               │
categories                        │
  │                               │
  │ 1:N                           │
  ▼                               │
user_preferences ◄────────────────┘
```

### 3.2 核心表结构

| 表名 | 核心字段 | 设计要点 |
|------|----------|----------|
| `users` | id, username, password_hash, nickname, email | BCrypt 存储密码，username 唯一索引 |
| `categories` | id, name, display_name, icon, sort_order | 预置 5 条数据（吃/喝/玩/住/其他） |
| `options` | id, user_id(FK), category_id(FK), name, tags, is_active | 软删除（is_active），复合索引 (user_id, category_id) |
| `decisions` | id, user_id(FK), category_id(FK), chosen_option_id(FK), context_json | context_json 存决策上下文（JSON 类型） |
| `feedback` | id, decision_id(FK, UNIQUE), user_id(FK), rating(1-3) | 每个决策只能有一条反馈 |
| `user_preferences` | id, user_id(FK), category_id(FK), tag, weight(DECIMAL 5,4) | 唯一约束 (user_id, category_id, tag) |

### 3.3 面试常见问题

**Q: 为什么用软删除而不是物理删除？**
> A: 用户的选项可能被历史决策引用。物理删除会导致 `decisions` 表的外键断裂，查询历史时出现数据不一致。软删除通过 `is_active` 字段标记，既保留了历史完整性，又不影响正常使用时的查询。

**Q: 为什么 decisions 表用 context_json 存 JSON？**
> A: 决策上下文包含时间、过滤条件等动态信息，结构不固定。用 JSON 字段存储比建额外的关联表更灵活，也便于后续扩展上下文维度。Hibernate 6 通过 `@JdbcTypeCode(SqlTypes.JSON)` 支持 PostgreSQL 的 JSON 类型映射。

**Q: user_preferences 的权重范围为什么是 [0.05, 0.95]？**
> A: 这是"探索-利用"（Exploration-Exploitation）的经典设计。如果权重可以到 0，某个标签的选项将永远不会被选中，用户就失去了发现新偏好的机会。0.05 保证了最低 5% 的探索概率，0.95 避免了 100% 确定性。

---

## 四、核心算法详解

### 4.1 加权随机决策引擎

**算法流程**（`DecisionServiceImpl.java`）：

```
输入: userId, categoryId, optionIds(可选)
输出: DecisionVO { chosenOption, alternates, decidedAt }

1. 查询该用户在该分类下的所有活跃选项
2. 若传入 optionIds，仅保留指定选项（决策库选择模式）
3. 若传入 preferenceFilters，按标签过滤
4. 查询近 N 天已选选项，从候选中排除（避免重复）
5. 对每个候选选项计算权重:
   weight = 1.0
   for tag in option.tags:
       weight *= (0.5 + userPreferenceWeight[tag])
   if option recently chosen:
       weight *= 0.3
   weight += random(0, 0.1)  // 微小扰动
6. 累积权重法随机选择:
   total = sum(weights)
   rand = random(0, total)
   cumulative = 0
   for option in candidates:
       cumulative += option.weight
       if cumulative >= rand:
           return option
7. 选权重最高的 2 个作为备选方案
8. 持久化决策记录，返回结果
```

**时间复杂度**: O(n × m)，n 为候选选项数，m 为平均标签数。

**面试追问**：

**Q: 为什么用乘法模型而不是加法模型计算权重？**
> A: 乘法模型让标签权重之间产生"协同效应"。比如一个选项同时有"辣"和"快"两个高权重标签，乘法模型会让它的权重显著高于只含一个标签的选项，更符合用户的复合偏好。加法模型则会让各标签独立贡献，区分度不够。

**Q: 为什么加随机扰动？**
> A: 如果没有扰动，权重完全相同的两个选项会永远按固定顺序被选中（取决于遍历顺序）。0.1 的扰动很小（相对于 1.0 的基础权重），不会显著影响概率分布，但能打破确定性。

### 4.2 偏好学习算法

```
反馈类型        权重调整公式                  效果
──────────────────────────────────────────────────
满意 (3)     w += 0.05 × (1 - w)         趋向 1.0
中性 (2)     不变                          保持
不满意 (1)   w -= 0.05 × w               趋向 0.0
```

**关键特性**：
- **非线性收敛**：权重越接近极值，变化越慢（导数趋近 0），避免过度拟合
- **上下界钳制**：[0.05, 0.95]，保证探索空间
- **增量学习**：每次只调一个标签的权重，不需要全局重算

**面试追问**：

**Q: 这个学习率 0.05 是怎么确定的？会不会太慢？**
> A: 0.05 是经验值，平衡了学习速度和稳定性。如果太大（如 0.2），单次反馈就会大幅改变偏好，系统对噪声太敏感。如果太小（如 0.01），需要很多次反馈才能看出变化。0.05 意味着大约 14 次连续满意反馈可以将权重从 0.5 提升到 0.85，学习曲线合理。

---

## 五、Spring Boot 后端架构

### 5.1 分层架构

```
Controller → Service → Repository → Entity
    │           │
    ↓           ↓
  DTO/VO    业务逻辑
(数据传输)  (事务管理)
```

| 层级 | 职责 | 技术实现 |
|------|------|----------|
| Controller | 接收请求、参数校验、返回统一响应 | `@RestController` + Bean Validation |
| Service | 业务逻辑、事务管理 | `@Service` + `@Transactional` |
| Repository | 数据访问、查询方法 | Spring Data JPA + 自定义 JPQL |
| Entity | 数据库映射 | JPA `@Entity` + Hibernate |

### 5.2 安全认证（JWT）

```
请求流程:
Client → Authorization: Bearer <token> → JwtAuthFilter
    → 解析 token → 提取 userId → 设置 SecurityContext
    → Controller 获取当前用户: auth.getPrincipal()

Token 结构:
Header:  { "alg": "HS256" }
Payload: { "sub": "123", "username": "test", "iat": ..., "exp": ... }
Signature: HMAC-SHA256(header + payload, secret)
```

**面试追问**：

**Q: JWT 和 Session 的区别？为什么选 JWT？**
> A: Session 有状态，服务端需要存储会话信息，多实例部署时需要共享 Session（如 Redis）。JWT 无状态，Token 自包含用户信息，服务端只需验证签名，天然支持水平扩展。这个项目部署在 Render 免费层（单实例），但 JWT 的无状态特性让未来扩展更方便。

**Q: JWT 的缺点是什么？怎么解决？**
> A: JWT 无法主动失效（比如用户修改密码后，旧 Token 仍然有效直到过期）。解决方案可以是：1) 设置较短过期时间 + Refresh Token 机制；2) 维护 Token 黑名单（Redis）；3) 在 Token 中加入版本号，修改密码时更新版本。当前项目用 24 小时过期，对实习项目来说足够。

### 5.3 异常处理

```java
@RestControllerAdvice
public class GlobalExceptionHandler {
    @ExceptionHandler(BusinessException.class)   // 业务异常
    @ExceptionHandler(MethodArgumentNotValidException.class)  // 参数校验
    @ExceptionHandler(Exception.class)           // 兜底异常
}
```

所有异常统一返回 `{ code: 0, msg: "...", data: null }`，前端只需判断 `code` 字段。

### 5.4 多环境配置

```yaml
application.yml          # 公共配置 (Jackson, JPA)
application-dev.yml      # 本地: MySQL, show-sql=true, LLM=MiMo
application-prod.yml     # 生产: PostgreSQL, LLM from env vars
```

通过 `spring.profiles.active` 切换，生产环境的敏感信息（数据库密码、JWT Secret、API Key）全部通过环境变量注入。

---

## 六、AI 大模型集成

### 6.1 架构设计

```
AiController → LlmService → LlmConfig → HTTP Client → MiMo API
                     │
                     ├── getSmartRecommendation()  // 智能推荐
                     ├── suggestNewOptions()        // 建议新选项
                     ├── analyzePreferences()       // 偏好分析
                     └── explainDecision()          // 决策解释
```

### 6.2 关键实现

- **API 协议**：OpenAI Chat Completions 兼容格式，支持任意 LLM 提供商
- **重试机制**：3 次重试 + 指数退避（2s, 4s, 6s），应对跨境网络不稳定
- **超时控制**：60 秒，适配免费模型的推理延迟
- **Prompt 工程**：针对不同场景设计专用 prompt，控制输出格式

### 6.3 面试追问

**Q: 为什么用同步调用而不是异步？**
> A: LLM 调用是用户主动触发的（点击 AI 推荐按钮），需要等待结果返回才能展示。同步调用实现简单，超时控制直观。如果要优化体验，可以用 WebSocket 推送流式响应（SSE），但对当前场景来说同步就够了。

**Q: 如果 LLM API 不可用怎么办？**
> A: `LlmServiceImpl` 中 `chat()` 方法在异常时返回 `null`，Controller 层检查返回值后给用户一个友好提示（"AI 服务暂时不可用"）。核心的随机决策功能不依赖 LLM，降级后仍可正常使用。

---

## 七、部署与运维

### 7.1 Docker 多阶段构建

```dockerfile
# Stage 1: Build
FROM maven:3.9-eclipse-temurin-17 AS build
COPY random-backend/pom.xml .
RUN mvn dependency:go-offline -B          # 缓存依赖层
COPY random-backend/src ./src
RUN mvn clean package -DskipTests -B

# Stage 2: Runtime
FROM eclipse-temurin:17-jre-alpine        # 精简运行时镜像
COPY --from=build /app/target/*.jar app.jar
COPY start.sh /app/start.sh
CMD ["/app/start.sh"]
```

**面试追问**：

**Q: 为什么用多阶段构建？**
> A: 第一阶段包含 Maven、JDK、源码、依赖缓存，镜像约 800MB。第二阶段只需要 JRE + JAR，约 200MB。多阶段构建让最终镜像体积减少 75%，加快部署速度，也减小了攻击面。

### 7.2 生产环境关键配置

```sh
# start.sh
exec java -Duser.timezone=Asia/Shanghai -Dserver.port=${PORT:-10000} -jar app.jar
```

- **时区**：强制使用北京时间，避免服务器 UTC 时区导致的时间偏差
- **端口**：通过 `$PORT` 环境变量注入，适配 Render/Railway 等 PaaS 平台
- **健康检查**：`/api/health` 端点，Render 用它判断服务是否就绪

### 7.3 踩过的坑与解决方案

| 问题 | 根因 | 解决方案 |
|------|------|----------|
| Railway 部署 502 | `$PORT` 在 Dockerfile CMD 中不展开 | 创建 `start.sh` 包裹 shell |
| PostgreSQL JSON 列报错 | Hibernate 发送 varchar 给 json 类型列 | `@JdbcTypeCode(SqlTypes.JSON)` |
| Render 冷启动超时 | 免费层 30-60 秒冷启动 | Axios 超时增加到 60s |
| AI 时间显示"深夜" | Render 用 UTC，比北京慢 8 小时 | JVM 参数 `-Duser.timezone=Asia/Shanghai` |
| AI 功能间歇性失败 | 共享 HttpClient 连接池 + 跨境网络不稳定 | 每次创建新 HttpClient + synchronized + 3 次重试 |

---

## 八、项目中体现的技术能力

### 8.1 架构设计能力
- **分层架构**：Controller → Service → Repository，职责清晰
- **统一响应格式**：`{ code, msg, data }`，前端处理逻辑统一
- **全局异常处理**：`@RestControllerAdvice` 统一捕获，避免异常信息泄露
- **多环境配置**：Profile 机制隔离开发/生产配置

### 8.2 算法设计能力
- **加权随机算法**：累积权重法，O(n) 时间复杂度
- **偏好学习**：增量式在线学习，非线性收敛
- **探索-利用平衡**：权重钳制 [0.05, 0.95]

### 8.3 工程实践能力
- **安全**：BCrypt 密码哈希 + JWT 无状态认证 + Spring Security 权限控制
- **数据库**：索引优化、软删除、JSON 字段、多数据库兼容
- **容器化**：Docker 多阶段构建、精简镜像
- **DevOps**：CI/CD（GitHub → Render 自动部署）、健康检查

### 8.4 问题解决能力
- **跨时区问题**：从 JVM 层到代码层双重保障
- **跨境网络问题**：重试 + 指数退避 + 新建连接
- **数据库兼容性**：MySQL → PostgreSQL 平滑迁移

### 8.5 AI 工程能力
- **LLM 集成**：OpenAI 兼容协议、Prompt 工程
- **降级策略**：LLM 不可用时核心功能不受影响
- **生产级调用**：超时控制、重试机制、错误日志

---

## 九、面试高频问题准备

### 9.1 Spring Boot 相关

**Q: Spring Boot 的自动配置原理？**
> A: `@SpringBootApplication` 包含 `@EnableAutoConfiguration`，通过 `spring.factories`（或 Spring Boot 3 的 `AutoConfiguration.imports`）加载自动配置类。每个配置类用 `@ConditionalOnClass`、`@ConditionalOnMissingBean` 等条件注解判断是否生效。比如项目中引入了 `spring-boot-starter-data-jpa`，Hibernate、DataSource、EntityManager 的配置就自动生效。

**Q: Spring Security 的过滤器链？**
> A: Spring Security 本质是一串 Filter。核心链路：`SecurityContextPersistenceFilter` → `UsernamePasswordAuthenticationFilter` → `ExceptionTranslationFilter` → `FilterSecurityInterceptor`。我们在 `UsernamePasswordAuthenticationFilter` 之前插入了自定义的 `JwtAuthFilter`，实现无状态 Token 认证。

### 9.2 数据库相关

**Q: JPA 和 MyBatis 的区别？为什么选 JPA？**
> A: JPA 是 ORM 框架，自动生成 SQL，适合 CRUD 密集的业务。MyBatis 是 SQL 映射框架，手写 SQL，适合复杂查询。这个项目以标准 CRUD 为主，JPA 的 `findByXxx` 方法和 `@Query` 注解足够，开发效率更高。

**Q: Hibernate 的 N+1 问题怎么解决？**
> A: 关联查询时默认用懒加载（`FetchType.LAZY`），访问关联属性时会发额外的 SQL。解决方案：1) `@EntityGraph` 指定关联加载策略；2) JPQL 的 `JOIN FETCH`；3) `@BatchSize` 批量加载。项目中 Decision 实体的 `user`、`category`、`chosenOption` 都是懒加载，查询历史时通过 `@Query` + `JOIN FETCH` 一次性加载。

### 9.3 并发与性能

**Q: 如果两个用户同时提交反馈，偏好权重会不会出问题？**
> A: 当前用 `synchronized` 保证单机线程安全。生产环境如果多实例部署，需要分布式锁（Redis SETNX）或数据库乐观锁（`@Version` 字段）。对实习项目来说，单机 + 事务隔离级别（READ COMMITTED）已经足够。

**Q: 怎么优化大量选项时的查询性能？**
> A: 1) 数据库层面：`(user_id, category_id)` 复合索引覆盖主要查询；2) 应用层面：分页查询历史决策；3) 缓存：对不常变的分类数据可以用 Redis 缓存。

### 9.4 设计模式

项目中用到的设计模式：
- **策略模式**：`LlmService` 接口 + `LlmServiceImpl` 实现，可替换不同 LLM 提供商
- **模板方法**：所有 Entity 的 `@PrePersist` / `@PreUpdate` 回调
- **建造者模式**：VO 对象使用 `@Builder`
- **过滤器链**：Spring Security 的 Filter Chain
- **工厂方法**：Spring 的 `@Bean` 工厂

---

## 十、可扩展方向（面试加分项）

如果面试官问"你打算怎么改进这个项目"，可以从以下角度回答：

1. **Redis 缓存**：缓存分类数据、用户偏好，减少数据库查询
2. **消息队列**：LLM 调用改为异步，用 RabbitMQ/Kafka 解耦
3. **Elasticsearch**：决策历史全文搜索
4. **微服务拆分**：AI 服务独立部署，支持多模型切换
5. **监控告警**：Prometheus + Grafana 监控 API 响应时间和错误率
6. **单元测试**：JUnit 5 + Mockito 覆盖核心算法
7. **API 文档**：Swagger/OpenAPI 自动生成接口文档
8. **WebSocket**：LLM 流式响应，提升 AI 推荐的用户体验

---

## 十一、项目数据总结

| 维度 | 数据 |
|------|------|
| 后端 Java 文件 | 56 个 |
| 前端源文件 | 44 个 |
| 数据库表 | 6 张 |
| API 接口 | 20 个 |
| 代码总量 | 4000+ 行 |
| 第三方集成 | MiMo LLM API |
| 部署平台 | Vercel + Render + PostgreSQL |
| 开发周期 | 全栈独立完成 |

---

*文档生成时间：2026-05-01*
